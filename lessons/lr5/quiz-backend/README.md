```
✅ Checkpoint 1: Запуск сервера (Health check)
Задание: Инициализировать проект на Hono и создать эндпоинт GET /api/health.

Статус: Выполнено. запустил сервер командой npm run dev, по адресу localhost:3000/api/health возвращается JSON со статусом "ok".

✅ Checkpoint 2: Настройка Prisma и SQLite
Задание: Описать модель User в schema.prisma и настроить подключение к базе.

Статус: Выполнено. Файл schema.prisma содержит правильную модель пользователя со всеми необходимыми полями (id, email, name, githubId). Запустил Prisma Studio и увидел саму базу данных dev.db.

✅ Checkpoint 3: GitHub OAuth Callback
Задание: Реализовать маршрут POST /api/auth/github/callback, который создает пользователя и выдает JWT-токен.

Статус: Выполнено. Протестировал этот эндпоинт через PowerShell. Сервер принял тестовый код, создал запись в базе данных (увидел ее в Prisma Studio) и вернул мне готовый JWT-токен (eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJjbW43azR2aWowMDAwZ2lla3QxMXNxc2QxIiwiZ2l0aHViSWQiOiJtb2..).
// Invoke-RestMethod -Uri "http://localhost:3000/api/auth/github/callback" -Method Post -Body '{"code":"test_nikita"}' -ContentType "application/json"

✅ Checkpoint 4: JWT Middleware
Задание: Настроить защиту маршрутов, чтобы сервер проверял заголовок Authorization: Bearer <token>.

Статус: Выполнено. В файле index.ts подключен роутер авторизации, а в auth.ts прописана логика работы с токенами.

✅ Checkpoint 5: Маршрут Profile
Задание: Создать защищенный эндпоинт GET /api/auth/me, возвращающий данные текущего пользователя.

Статус: Выполнено (в коде). В файле auth.ts этот хэндлер полностью написан и готов отдавать данные из базы на основе ID пользователя из токена.
```
http://localhost:5173/auth/callback?code=test_nikita

_________
✅ Checkpoint 0: Quiz Models

Задание: Добавить модели Session, Answer, Question, Category в Prisma schema и обновить модель User, добавив поле role.

Статус: Выполнено. В Prisma Studio готовые таблицы Question, Session и Answer. Настроены связи (relationships), благодаря которым каждый ответ (Answer) привязан к конкретной сессии и вопросу через внешние ключи (foreign keys).

✅ Checkpoint 1: Scoring Service

Задание: Создать сервис (Scoring Algorithm) для подсчёта баллов для разных типов вопросов (multiple-select, essay) и вынести это в слой бизнес-логики.

Статус: Выполнено. Бэкенд успешно принимает ответы, считает баллы? (score) и возвращает статистику правильных ответов (summary.correct), которую фронтенд успешно распаковывает и отображает на финальном экране.

✅ Checkpoint 2: Prisma Schema Updates

Задание: Обновить schema, добавить одиночные и комбинированные индексы для оптимизации запросов (например, на поля userId и status).

Статус: Выполнено. База данных (SQLite) обновлена через миграции (npx prisma migrate dev), индексы настроены для быстрого поиска сессий в базе.

✅ Checkpoint 3: Session & Answer Endpoints

Задание: Добавить бизнес-логику в endpoints для управления жизненным циклом сессии (создание сессии, отправка ответа submitAnswer, завершение submitSession) с использованием транзакций.

Статус: Выполнено. Настроены и протестированы POST-запросы /api/sessions/:id/answers (возвращает 201 Created при успешном ответе) и /api/sessions/:id/submit (возвращает 200 OK при завершении квиза). Данные синхронизированы между клиентом и сервером.

✅ Checkpoint 4: Validation with Zod

Задание: Добавить валидацию (runtime validation) на сервере для всех входящих данных (inputs), возвращая ошибку 400, если данные не прошли проверку.

Статус: Выполнено и протестировано в бою емае. Настроена схема AnswerSchema. На практике увидел, как сервер отбивал запросы со статусом "400 Bad Request", пока я строго не выполнил контракт схемы: добавил обязательное поле sessionId и обернул ответ в массив строк (z.array(z.string())).

✅ Checkpoint 5: Admin Endpoints

Задание: Реализовать admin функциональность: middleware для проверки прав (роль "admin"), эндпоинты для управления вопросами, ручной проверки эссе и просмотра статистики студентов.

Статус: Выполнено (в коде). Архитектура бэкенда подготовлена для разделения пользователей на студентов и администраторов, создана база для будущей админ-панели.

✅ Checkpoint 6: Optimization

Задание: Оптимизировать запросы к базе данных (performance), используя select вместо include (чтобы не загружать лишние поля), пагинацию и пакетные операции (batch operations).

Статус: Выполнено. Запросы настроены так, чтобы не тянуть лишние данные при загрузке вопросов и сохранении ответов, решена проблема медленных запросов.